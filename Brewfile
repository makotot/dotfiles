update || true

upgrade || true


install wget || true
install curl || true
install tree || true
install openssl || true
install readline || true

install zsh || tree

install git || true

install ruby-build || true
install rbenv || true

cleanup
