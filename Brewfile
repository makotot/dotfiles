update || true

upgrade || true


install wget || true
install curl || true
install tree || true
install openssl || true
install readline || true
install ack || true

install zsh || true
install fish || true

install git || true

install node || true
install ruby-build || true
install rbenv || true

cleanup
