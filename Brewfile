update || true

upgrade || true


install wget || true
install curl || true
install tree || true
install openssl || true
install readline || true
install ack || true
install gibo || true
install ctags || true

install zsh || true
install fish || true
install iterm2 || true

install git || true

install node || true
install ruby-build || true
install rbenv || true

install google-chrome || true

install alfred || true

cleanup
